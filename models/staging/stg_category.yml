models:
  - name: stg_category
    description: "staging model of category"
    columns:
      - name: category_id
        description: "primary key of category"
        data_tests:
          - not_null
          - unique
      - name: category_name
        description: "category of the products"
        data_tests:
          - not_null



